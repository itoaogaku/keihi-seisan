# 経費精算PDF作成アプリ

個人のクレジットカード明細から、会社ごとの経費精算PDFを作成し、
Googleスプレッドシートに保存できるWebアプリです。

## 1. システム構成

サーバー側にDBを持たず、各ユーザーのGoogleスプレッドシート宛にブラウザから
直接データを送る構成にすることで、共通の1つのURLを同僚と共有しても
データが混ざらないマルチテナント構成を実現しています。

```
                共通のURL (Vercel)
                        │
        ┌───────────────┴───────────────┐
        │        Next.js アプリ           │
        │  (フロントエンドのみ・DBなし)     │
        └───────────────┬───────────────┘
                         │ ブラウザのlocalStorageに
                         │ 「自分のGAS URL」を保存
        ┌────────────────┼────────────────┐
        │                │                │
   自分のブラウザ      同僚Aのブラウザ    同僚Bのブラウザ
        │                │                │
        ▼                ▼                ▼
  自分のGAS URL      同僚AのGAS URL   同僚BのGAS URL
        │                │                │
        ▼                ▼                ▼
  自分のスプレッドシート 同僚Aのシート    同僚Bのシート
```

- **フロントエンド(Next.js, Vercelにデプロイ)**: CSV取り込み・仕分け・集計・PDF出力を担当。
- **設定(接続設定)**: 各ユーザーが自分のGAS WebアプリURLをブラウザのlocalStorageに保存。
- **バックエンド(GAS)**: 確定した経費データをPOSTで受け取り自分のスプレッドシートに追記する処理と、
  保存済みデータをGETで一覧取得する処理(`?action=list`)を持つ薄い層。
- **データベース**: 各ユーザー自身のGoogleスプレッドシート(サーバー側は一切保持しない)。

## 2. 画面設計(ワイヤーフレーム)

### メイン画面 `/`

```
┌───────────────────────────────────────────────────┐
│ 経費精算PDF作成                           [⚙ 設定] │
├───────────────────────────────────────────────────┤
│ 1. 明細の取り込み                                    │
│  [ CSVをドラッグ&ドロップ / ファイル選択 ]           │
│  [ または明細をテキストで貼り付け ] [取り込む]        │
├───────────────────────────────────────────────────┤
│ 現金決済の追加                                       │
│  支払日:[__] 内容:[____] 金額:[____] メモ:[____] [追加]│
├───────────────────────────────────────────────────┤
│ 2. 集計期間(締め日)の設定                            │
│  締め日:[15] 対象年月:[2026-08] → 期間: 07/16〜08/15  │
├───────────────────────────────────────────────────┤
│ 明細の仕分け             全32件  未仕分け 5件         │
│ ┌──────┬────┬───────┬──────┬───────┬──────────┬──┐ │
│ │利用日 │方法│ 内容   │ 金額  │ メモ  │仕分け(選択)│  │ │
│ ├──────┼────┼───────┼──────┼───────┼──────────┼──┤ │
│ │08/01 │カード│〇〇文具│¥3,200│[___] │[組織A ▼] │🗑│ │
│ │08/02 │現金 │タクシー│¥1,500│[___] │[組織B ▼] │🗑│ │
│ │ …                                                  │ │
│ └───────────────────────────────────────────────┘ │
├───────────────────────────────────────────────────┤
│ 3. 集計結果                                          │
│   合計請求額: ¥XX,XXX                                │
│   [組織A: ¥.. ] [組織B: ¥.. ] [組織C: ¥.. ] [組織D]  │
├───────────────────────────────────────────────────┤
│ 4. PDF出力・保存                                     │
│   申請者名:[____]  備考:[____]                       │
│   [ 経費精算PDFを出力 ]  [ スプレッドシートに保存 ]    │
└───────────────────────────────────────────────────┘
```

### 履歴画面 `/history`

期間による絞り込みは行わず、スプレッドシートに保存済みの明細をすべて表示します。

```
┌───────────────────────────────────────────────────┐
│ ← 戻る   保存履歴                          [⚙ 設定] │
│ 期間による絞り込みはありません。                      │
├───────────────────────────────────────────────────┤
│ 一覧  全128件 合計¥XXX,XXX      [更新] [CSVで書き出す]│
│ ┌────────┬─────┬────┬────────┬──────┬────┬──────┐ │
│ │保存日時 │利用日│方法│請求先組織│内容  │メモ│ 金額 │ │
│ ├────────┼─────┼────┼────────┼──────┼────┼──────┤ │
│ │08/10 12:00│08/01│カード│組織A │〇〇文具│ -  │¥3,200│ │
│ │ …                                              │ │
│ └────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────┘
```

「CSVで書き出す」は、一覧に表示されている全件をそのままCSVファイルとしてダウンロードします。

### 設定画面 `/settings`

```
┌───────────────────────────────────────────────────┐
│ ← 戻る   設定(接続設定)                              │
│ このブラウザにのみ保存され、他の人とは共有されません。   │
├───────────────────────────────────────────────────┤
│ GAS WebアプリURL                                     │
│ [ https://script.google.com/macros/s/xxx/exec  ]    │
│ [ 保存 ]  [ 接続テスト ]  [ 設定を削除 ]              │
└───────────────────────────────────────────────────┘
```

### 出力されるPDF(1枚の経費精算書)

```
┌───────────────────────────────────────────────────┐
│ 経費精算書              申請者: 山田太郎  作成日: ..  │
│ 対象期間: 2026/07/16〜2026/08/15                     │
│ ────────────────────────────────────────────────  │
│ 合計請求額                              ¥XX,XXX      │
│ ────────────────────────────────────────────────  │
│ 組織別内訳                                          │
│  請求先組織 │ 件数 │ 金額                            │
│  組織A     │ 5件  │ ¥XX,XXX                         │
│  組織B     │ 3件  │ ¥XX,XXX  …                      │
│ ────────────────────────────────────────────────  │
│ 明細一覧                                            │
│  利用日 │ 請求先組織 │方法 │ 内容      │メモ│ 金額     │
│  08/01  │ 組織A     │カード│〇〇文具店 │  - │ ¥3,200  │
│  …                                                  │
└───────────────────────────────────────────────────┘
```

## 3. 使い方

1. カード会社サイトからダウンロードしたCSV(または明細のコピペ)を取り込む。現金決済は「現金決済の追加」から1件ずつ追記する。
2. 締め日と対象年月を指定し、集計期間を確認する。
3. 各明細の「仕分け」列で請求先組織(または「除外」)を選択する。必要に応じてメモも入力する。
4. 集計結果を確認し、「経費精算PDFを出力」でPDFをダウンロードする。
5. 「スプレッドシートに保存」で、設定済みのGASを通じて自分のスプレッドシートに保存する
   (初回は「設定」画面でのGAS URL登録が必要。手順は [`gas/README.md`](./gas/README.md) を参照)。
6. 「履歴」タブでは、保存済みの全データを期間の絞り込みなしで一覧表示し、
   そのまま全件をCSVファイルとして書き出せる。

CSVの列名はカード会社ごとに異なりますが、`利用日`/`ご利用日`/`日付`、
`利用店名`/`摘要`/`内容`、`利用金額`/`金額` などの一般的な表記を自動認識します。
認識できるヘッダーが見つからない場合は「先頭列から日付・内容・金額」とみなして読み込みます。

## 4. 技術構成

| 領域 | 技術 |
| --- | --- |
| フロントエンド | Next.js 14 (App Router) + TypeScript + Tailwind CSS |
| UIコンポーネント | shadcn/ui スタイルの自前実装コンポーネント |
| CSV解析 | PapaParse |
| PDF出力 | jsPDF + html2canvas(ブラウザ描画をそのまま画像化するため、日本語フォント埋め込みが不要) |
| バックエンド | Google Apps Script(Webアプリとしてデプロイ、ユーザーごとに個別) |
| データベース | 各ユーザー自身のGoogleスプレッドシート |
| ホスティング | Vercel |

## 5. セットアップ・開発

```bash
npm install
npm run dev       # http://localhost:3000
npm run build     # 本番ビルド確認
```

### Vercelへのデプロイ

このリポジトリをVercelにインポートしてデプロイするだけで動作します。
サーバー側の環境変数やDB設定は不要です(GAS URLは各ユーザーのブラウザに保存されるため)。

### 同僚が使い始めるとき

1. VercelのURLにアクセスする。
2. [`gas/README.md`](./gas/README.md) の手順で、自分のスプレッドシート宛にGASをデプロイする。
3. アプリの「設定」画面で発行されたGAS URLを登録する。

これにより、共通のURLを使いながらも、各自のデータは各自のスプレッドシートにのみ保存されます。

## 6. ディレクトリ構成

```
app/
  page.tsx            メイン画面(取り込み〜PDF出力〜保存)
  history/page.tsx     履歴画面(保存済み全データの一覧・CSV書き出し)
  settings/page.tsx    設定画面(GAS URL登録)
components/
  csv-uploader.tsx      CSV/貼り付け取り込みUI
  cash-entry-form.tsx   現金決済の手動追加フォーム
  transaction-table.tsx 明細の仕分け表(メモ入力含む)
  period-selector.tsx   締め日・対象年月の指定
  summary-panel.tsx     集計結果の表示
  ui/                   共通UIパーツ
lib/
  csv-parser.ts         CSV/貼り付けテキストの解析
  encoding.ts             CSVファイルの文字コード自動判定(UTF-8/Shift_JIS)
  aggregate.ts           締め日ベースの期間計算・組織別集計
  pdf-template.ts        PDF化するHTMLの組み立て
  pdf-generator.ts       jsPDF + html2canvasでのPDF生成
  gas-client.ts           GAS Webアプリへの送信・接続テスト・履歴取得
  csv-export.ts           履歴一覧のCSV書き出し
  storage.ts              GAS URL・下書き明細のlocalStorage管理
  types.ts                型定義・請求先組織の定義
gas/
  Code.gs                GAS側スクリプト(doGet/doPost、履歴一覧取得)
  README.md               GASのデプロイ手順
```
